FIND all variables and constants, only after it set stack pointer
ADD support for arrays of integer
ADD subroutines GOSUB...RETURN
ADD cycles
   FOR x = 2 TO 5 [STEP 3]
   ...
   NEXT
ADD float support
WRITE basic interpreter